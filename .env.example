# GitHub Configuration
#
# Authentication (choose one):
# Option 1 (Recommended): Use GitHub CLI - run `gh auth login` (no .env needed!)
# Option 2: Set GITHUB_TOKEN below

# GITHUB_TOKEN=your_github_personal_access_token_here

# Optional: Organization to query for issues
# GITHUB_ORG=your_organization_name

# Optional: Username (auto-detected if using gh CLI)
# GITHUB_USERNAME=your_github_username
